# যা পরিবর্তন করা হয়েছে

এই ৫টা ফাইল বদলানো হয়েছে — এগুলো দিয়ে তোমার GitHub repo-র একই নামের
ফাইলগুলো replace করে দাও, তারপর commit + push করলেই কাজ শেষ:

- `index.html`
- `render.js`
- `admin.js`
- `admin.html`
- `subpage.js`

রঙ, CSS, ডিজাইন — কিছুই বদলানো হয়নি। শুধু ভেতরের static content আর
কিছু JavaScript লজিক বদলেছে।

## ১. মূল বাগ ফিক্স — এখন delete করলে সত্যিই delete হবে

আগে `render.js`-এ এমন কোড ছিল যে Firestore-এ কোনো কালেকশন খালি হলে
(অর্থাৎ admin থেকে সব ডিলিট করে দিলে) ওয়েবসাইট আগের static/hardcoded
content-ই দেখাতে থাকত। এই কোডটাই সরানো হয়েছে —

- নোটিশ, সময়সূচি, কার্যকরী সদস্যবৃন্দ, উপদেষ্টা, সাধারণ সদস্যবৃন্দ,
  টিকার বার্তা, গ্যালারি, অঙ্গসংগঠন — এই সব সেকশনে এখন admin থেকে
  delete করলে সাথে সাথেই ওয়েবসাইট থেকেও মুছে যাবে। খালি থাকলে একটা
  ছোট "এখনো যোগ করা হয়নি" মেসেজ দেখাবে।
- `index.html`-এর ভেতরে যেসব hardcoded উদাহরণ ডেটা ছিল (৩৬ জন সদস্য,
  ১৩ জন উপদেষ্টা, ৭টা ছবি ইত্যাদি) — সব সরিয়ে ফেলা হয়েছে।

## ২. admin.js-এও একটা লুকানো সমস্যা ছিল

Admin panel-এ লগইন করার সময় কোড **automatically** কিছু default
ticker message আর gallery ছবি Firestore-এ আবার বসিয়ে দিত, যদি সেই
কালেকশন খালি থাকত। মানে তুমি যদি সব ticker/gallery মুছে ফেলতে, পরের
বার admin panel-এ ঢুকলেই সেগুলো আবার ফিরে আসত! এই auto-reseed লজিক
পুরোপুরি সরানো হয়েছে।

## ৩. দুইটা নতুন সেকশন যোগ হলো — আগে এগুলো একদমই static ছিল

- **অনুষ্ঠান (Events)** — আগে `events-grid`-এ ৬টা কার্ড সরাসরি HTML-এ
  লেখা ছিল, কোনো Firebase সংযোগ ছিল না। এখন admin panel-এ নতুন
  "🎉 অনুষ্ঠান" ট্যাব যোগ হয়েছে — এখান থেকে অনুষ্ঠান add/edit/delete
  করা যাবে (আইকন, শিরোনাম, বিবরণ, ক্রম)।
- **আমাদের সম্পর্কে (About)** — heading, ৪টা প্যারাগ্রাফ আর ৩টা
  পরিসংখ্যান (৫০+ বছর, ১৫০+ পরিবার, ৩৬+ সদস্য) আগে static ছিল। এখন
  admin panel-এ নতুন "📖 আমাদের সম্পর্কে" ট্যাব যোগ হয়েছে (settings-এর
  মতোই একটামাত্র ফর্ম — এডিট করা যাবে, delete করার দরকার নেই)।

এই দুইটা কালেকশনের (`events`, `about`) জন্য আলাদা কোনো Firestore rule
লাগবে না — SETUP-GUIDE.md-এ যে rule আগে থেকেই আছে সেটা সব কালেকশনের
জন্যই কাজ করে।

## ৪. প্রথমবার deploy করার পর কী করতে হবে

যেহেতু static content সরিয়ে ফেলা হয়েছে, deploy করার পর সাইট প্রথমে
অনেকটা খালি দেখাবে (নোটিশ, সময়সূচি, সদস্য, গ্যালারি, অঙ্গসংগঠন,
অনুষ্ঠান — সব কালেকশন-ই যদি আগে থেকে Firestore-এ পপুলেট করা না থাকে)।
তোমাকে admin panel থেকে এই ডেটাগুলো (আগে যা ছিল, দরকার হলে সেটাই আবার)
হাতে যোগ করে দিতে হবে — এটাই তো ছিল মূল উদ্দেশ্য, সব কিছু সরাসরি admin
থেকে control করা।

ব্যতিক্রম দুটো: **সাইট সেটিংস** আর **আমাদের সম্পর্কে** — এই দুইটা
singleton ফর্ম, প্রথমবার admin panel-এ লগইন করলে আগের মতোই একবার
default value দিয়ে auto-seed হয়ে যাবে, যাতে ফাঁকা ফর্ম না দেখতে হয়।
এরপর থেকে যা এডিট করবে সেটাই থাকবে।
