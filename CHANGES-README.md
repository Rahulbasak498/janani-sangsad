# আপডেট ৩ — অডিট ফিক্স

## ⚠️ এই আপডেট দেওয়ার পর অবশ্যই করবেন (না করলে শুভেচ্ছা বোর্ড কাজ করবে না)

1. Firebase Console → **Firestore Database → Rules** → এই repo-র `firestore.rules`-এর পুরো লেখা বসিয়ে **Publish**।
2. Admin প্যানেলে লগইন করে **⚙️ সাইট সেটিংস** খুলে আসল তথ্য দিন: বিকাশ/নগদ, ব্যাংক, ফোন, Facebook URL, ইমেইল।
   - যেটা ফাঁকা বা নকল (`0171…`, `0000-0000-0000`) থাকবে, ওয়েবসাইটে সেই লাইনটাই দেখাবে না।
3. **আসল QR:** `image/QR.jpg` একটা DEMO ছবি (ছবিতেই লেখা)। আপনার আসল QR অন্য নামে (যেমন `image/donation-qr.jpg`) আপলোড করে
   সাইট সেটিংসে "অনুদানের আসল QR ছবির URL"-এ সেই নাম দিন। যতক্ষণ না দিচ্ছেন, QR অংশ লুকানো থাকবে।
   পারলে `image/QR.jpg` ফাইলটা repo থেকে মুছে দিন।

## যা বদলানো হয়েছে

- **শুভেচ্ছা বোর্ড:** নকল নামের (বিজয় কুমার সাহা ইত্যাদি) বার্তা সরানো হয়েছে। নতুন বার্তা এখন "অপেক্ষমান" হয়ে Firestore-এ যায়;
  admin প্যানেলের নতুন **💬 শুভেচ্ছা বার্তা** ট্যাব থেকে "✓ অনুমোদন" দিলে তবেই দেখায়। Honeypot + ১ মিনিটের cooldown যোগ হয়েছে।
  পাঠানো ব্যর্থ হলে আর মিথ্যা "সফল" দেখায় না।
- **নকল/placeholder তথ্য:** ফোন, bKash, ব্যাংক, ফেসবুক, ইমেইল, রসিদ, QR — আসল মান না থাকলে লুকানো। Footer-এর আইকনও তাই (`href="#"` আর নেই)।
- **অনুদানে 📋 কপি বাটন** এবং "নম্বরের ধরন" (Personal/Merchant) ঘর।
- **"লোড হচ্ছে..." আটকে থাকা** বন্ধ: ডেটা না এলে "আবার চেষ্টা করুন" বার্তা।
- **অঙ্গসংগঠন** খালি থাকলে হোমপেজে পুরো সেকশন লুকায় (আলাদা পেজে দেখায়)।
- **Countdown-এর** স্থির ডিফল্ট ("০৭ দিন") সরিয়ে `--`; আসল সংখ্যা puja-calendar.js থেকে আসে (আগের মতোই)।
- **শেয়ার প্রিভিউ:** `og:image`/`canonical` এখন পূর্ণ URL, নতুন `image/og-share.jpg` (১২০০×৬৩০); সব সাবপেজে favicon/description।
- **ছবি ছোট করা:** `maa-durga.png` ১.৪MB→১১০KB, `মণ্ডপ সজ্জা.jpg` ২.৩MB→৪২০KB (একই নাম/পথ)।
- **Service worker:** cache v2 (পুরনো ভারী ছবি/DEMO QR মুছবে), ছবি এখন stale-while-revalidate।

---

# যা পরিবর্তন করা হয়েছে (আপডেট ২)

এই ৫টা ফাইল দিয়ে তোমার GitHub repo-র একই নামের ফাইলগুলো replace করে
দাও, commit + push করলেই কাজ শেষ:

- `index.html`
- `render.js`
- `admin.js`
- `admin.html`
- `subpage.js`

রঙ, CSS, ডিজাইন — কিছুই বদলানো হয়নি।

---

## রাউন্ড ১-এ যা হয়েছিল (আগের মেসেজে বলা হয়েছে)

- নোটিশ, সময়সূচি, সদস্য, উপদেষ্টা, টিকার, গ্যালারি, অঙ্গসংগঠন — delete
  করলে সত্যিই delete হয় এমন বানানো হয়েছিল।
- ticker/gallery-র auto-reseed বাগ ফিক্স হয়েছিল।
- "অনুষ্ঠান" আর "আমাদের সম্পর্কে" — নতুন দুইটা admin-controlled সেকশন
  যোগ হয়েছিল।

## রাউন্ড ২-এ (এই মেসেজে) যা যোগ হলো

### ১. Hero / Nav / Footer — যা static ছিল
- নেভিগেশনের ব্র্যান্ড নাম ("জননী সংসদ") আর "প্রতিষ্ঠা ১৯৭৪" ট্যাগলাইন
  — এখন সাইট সেটিংস থেকে আসে (`committeeName`, নতুন `foundingTagline`
  ফিল্ড)।
- **সাবপেজের নেভিগেশনেও** (about.html, schedule.html ইত্যাদি —
  `subpage.js`-এ আলাদা করে লেখা ছিল) একই ব্র্যান্ড নাম হার্ডকোড করা
  ছিল — সেটাও এখন dynamic।
- ফুটারের ব্র্যান্ড নাম, কপিরাইট লাইনের নাম — dynamic।
- ফুটারের কপিরাইট বছর (২০২৬) — এখন প্রতি বছর নিজে থেকেই আপডেট হবে
  (কোনো ইনপুট লাগবে না, JavaScript দিয়ে বর্তমান সাল বসে)।
- ফুটারের Facebook/Instagram/YouTube/Phone আইকনের লিংক — আগে `href="#"`
  ছিল (কোথাও যেত না) — এখন সাইট সেটিংসের নতুন ফিল্ড থেকে আসে।
- সাইটের ব্রাউজার ট্যাব টাইটেল (`<title>`) আর SEO বিবরণ
  (meta description / social share preview) — নতুন `siteTitle` ও
  `metaDescription` ফিল্ড দিয়ে এডিট করা যাবে।

### ২. Gallery — যা static ছিল
- গ্যালারির ছবিগুলো তো আগেই (রাউন্ড ১-এ) dynamic হয়েছিল।
- এবার গ্যালারি সেকশনের ওপরের ছোট লেবেল ("স্মৃতির পাতা"), মূল শিরোনাম
  ("গ্যালারি") আর সাবটাইটেল ("গত বছরগুলোর পূজার...") — এই তিনটাও এখন
  admin থেকে এডিট করা যাবে।

### ৩. "আরো কিছু" — সাইটের সবগুলো সেকশনের হেডিং
তোমার কথা মতো "কিছু static রাখা যাবে না" — তাই ১২টা সেকশনের প্রতিটার
উপরের ছোট লেবেল + শিরোনাম + সাবটাইটেল (মোট ৩৬টা টেক্সট) সব dynamic করে
দেওয়া হয়েছে:

আমাদের সম্পর্কে, সময়সূচি, অনুষ্ঠান, গ্যালারি, কার্যকরী সদস্যবৃন্দ,
উপদেষ্টা, সাধারণ সদস্যবৃন্দ, অঙ্গসংগঠন, অনুদান, নোটিশ, লোকেশন, যোগাযোগ

এর জন্য admin panel-এ নতুন **"🏷️ পেজের শিরোনাম"** ট্যাব যোগ হয়েছে —
একটামাত্র ফর্মে সব ৩৬টা ফিল্ড, প্রতিটা সেকশন অনুযায়ী নাম দেওয়া আছে
(যেমন: "গ্যালারি — শিরোনাম", "যোগাযোগ — সাবটাইটেল")।

---

## এখন যা static/hardcoded রয়ে গেছে (ইচ্ছাকৃতভাবে)

- **"Developed by Rahul Basak Santo"** — এটা ডেভেলপার ক্রেডিট লাইন,
  বদলাইনি। চাইলে এটাও editable বানিয়ে দিতে পারি।
- Contact কার্ডের আইকন-লেবেল যেমন "ফোন" / "ফেসবুক" / "ইমেইল",
  Hero-র বাটন লেবেল ("আমাদের সম্পর্কে", "পূজার সময়সূচি") — এগুলো
  navigation-এর মতো কাঠামোগত লেবেল, "content" এর বদলে UI-এর অংশ বলে
  ধরে রাখা হয়েছে। এগুলোও চাইলে dynamic করা যাবে, বলো।

## প্রথমবার deploy করার পর কী করতে হবে

- **সাইট সেটিংস**, **আমাদের সম্পর্কে**, আর **পেজের শিরোনাম** — এই তিনটা
  singleton ফর্ম প্রথমবার admin panel-এ লগইন করলে আগের static text
  দিয়েই auto-fill হয়ে যাবে (তাই এগুলোর জন্য নতুন করে কিছু টাইপ করতে
  হবে না, শুধু দরকার হলে এডিট করবে)।
- বাকি সব (নোটিশ, সময়সূচি, সদস্য, গ্যালারি ছবি, অঙ্গসংগঠন, অনুষ্ঠান)
  — এগুলো আগের মতোই হাতে admin থেকে যোগ করতে হবে, কারণ এগুলো তালিকা
  (list) — একবারে seed করলে delete করার পরেও ফিরে আসার ঝুঁকি থাকে,
  তাই ইচ্ছাকৃতভাবে auto-seed রাখা হয়নি।
